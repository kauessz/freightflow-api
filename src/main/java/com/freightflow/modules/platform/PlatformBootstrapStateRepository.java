package com.freightflow.modules.platform;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PlatformBootstrapStateRepository extends JpaRepository<PlatformBootstrapState, String> {
}
